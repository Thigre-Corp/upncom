<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Up'n'Com - La NewsLetter</title>
    <style>
        * {
            color: black;
            background-color: rgb(144, 255, 161);
        }
    </style>
</head>

<body>
    <h1>Up'n'Com - La NewsLetter</h1>
    <div>    
        Ceci est la premiere newsletter <br>
            watataï<br>
    {{subscriber.email}}<br>
    {{newsletter.contenu}}<br>
    </div>


    <p>Vous pourrez, si vous le souhaitez, vous désabonner à chaque instant grâce au lien suivant</p>
    <a href="{{ absolute_url( path('app_newsletters_unsubscribe', {id: subscriber.id, token: subscriber.token } ))}}"> {{ absolute_url(path('app_newsletters_unsubscribe', {id: subscriber.id, token:subscriber.token } ))}}</a>
            {# "{{ absolute_url(path('app_newsletters_validate'   , {id: subscriber.id, token: subscriber.token } ))}}" #}
</body>

</html>